{
  "name": "vault-insights",
  "version": "1.0.0",
  "description": "Generate daily summaries, aggregate todos, analyze topics, and visualize your vault content.",
  "main": "main.js",
  "scripts": {
    "build": "esbuild main.ts --bundle --external:obsidian --outfile=main.js --format=cjs --target=es2020",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src --ext .ts"
  },
  "keywords": [
    "obsidian",
    "plugin",
    "insights",
    "summary",
    "todo"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^16.11.6",
    "esbuild": "^0.17.0",
    "obsidian": "latest",
    "typescript": "^5.0.0"
  }
}
